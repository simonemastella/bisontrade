{
  "name": "@bisontrade/contracts",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.11",
    "@types/bun": "latest",
    "hardhat": "^2.23.0",
    "@vechain/sdk-core": "^1.2.0-rc.2",
    "@vechain/sdk-hardhat-plugin": "^1.2.0-rc.2",
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0",
    "typescript": "^5.8.3"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "compile": "bunx hardhat compile",
    "coverage": "bunx hardhat coverage",
    "ignition": "bunx hardhat ignition",
    "script": "bunx hardhat run",
    "build": "bun build typechain-types/index.ts --outdir dist --target bun --format esm --splitting && bun run tsc --emitDeclarationOnly --outDir dist"
  },
  "dependencies": {
    "solhint": "^5.0.5"
  },
  "module": "typechain-types/index.ts"
}